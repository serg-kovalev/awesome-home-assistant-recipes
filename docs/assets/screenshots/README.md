# Screenshots

Images used by the articles. They live inside `docs/` so MkDocs copies them to the site.

Reference them from an article as `../assets/screenshots/<name>.jpg`.

Before adding an image, check it for: device IDs, local keys, MAC addresses, real IP ranges, account
e-mail, cloud credentials, home coordinates. Crop or blur anything of that kind.
